<Window x:Class="SaleManagerApp.TablePicker"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sơ đồ bàn ăn" Height="400" Width="600"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    <Window.Resources>
        <Style x:Key="TableButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="90"/>
            <Setter Property="Height" Value="90"/>
            <Setter Property="Margin" Value="15"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Ellipse x:Name="Circle" Fill="{TemplateBinding Background}"/>
                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                <TextBlock Text="{TemplateBinding Tag}" 
                                         FontSize="12" 
                                         FontWeight="Normal"
                                         Foreground="{TemplateBinding Foreground}"
                                         HorizontalAlignment="Center"/>
                                <TextBlock Text="{TemplateBinding Content}" 
                                         FontSize="14" 
                                         FontWeight="SemiBold"
                                         Foreground="{TemplateBinding Foreground}"
                                         HorizontalAlignment="Center"
                                         Margin="0,2,0,0"/>
                            </StackPanel>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Circle" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="Sơ đồ bàn ăn" 
                   FontSize="16" 
                   FontWeight="SemiBold"
                   Foreground="#666"
                   Margin="230,0,0,10"/>

        <!-- Legend -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">

            <TextBlock Text="CÒN TRỐNG" FontSize="12" Foreground="#666" VerticalAlignment="Center" Margin="30,0,0,0"/>
            <Ellipse Width="25" Height="25" Fill="#E0E0E0" Margin="10,0,0,0"/>

            <TextBlock Text="ĐÃ CÓ KHÁCH" FontSize="12" Foreground="#666" VerticalAlignment="Center" Margin="30,0,0,0"/>
            <Ellipse Width="25" Height="25" Fill="#888888" Margin="10,0,0,0"/>
        </StackPanel>

        <!-- Tables Grid -->
        <WrapPanel Grid.Row="2" 
                   HorizontalAlignment="Left"
                   VerticalAlignment="Top">

            <!-- Bàn 1 - Còn trống (Xám nhạt) -->
            <Button Style="{StaticResource TableButtonStyle}"
                    Content="n chỗ"
                    Tag="Bàn 1"
                    Background="#E0E0E0"
                    Foreground="#888888"
                    Click="Table_Click"/>

            <!-- Bàn 2 - Còn trống (Xám nhạt) -->
            <Button Style="{StaticResource TableButtonStyle}"
                    Content="n chỗ"
                    Tag="Bàn 2"
                    Background="#E0E0E0"
                    Foreground="#888888"
                    Click="Table_Click"/>

            <!-- Bàn 3 - Đã có khách (Xám đậm, Disabled) -->
            <Button Style="{StaticResource TableButtonStyle}"
                    Content="n chỗ"
                    Tag="Bàn 3"
                    Background="#888888"
                    Foreground="#000000"
                    IsEnabled="False"/>

            <!-- Bàn 4 - Còn trống (Xám nhạt) -->
            <Button Style="{StaticResource TableButtonStyle}"
                    Content="n chỗ"
                    Tag="Bàn 4"
                    Background="#E0E0E0"
                    Foreground="#888888"
                    Click="Table_Click"/>

            <!-- Bàn 5 - Còn trống (Xám nhạt) -->
            <Button Style="{StaticResource TableButtonStyle}"
                    Content="n chỗ"
                    Tag="Bàn 5"
                    Background="#E0E0E0"
                    Foreground="#888888"
                    Click="Table_Click"/>

            <!-- Bàn 6 - Đã có khách (Xám đậm, Disabled) -->
            <Button Style="{StaticResource TableButtonStyle}"
                    Content="n chỗ"
                    Tag="Bàn 6"
                    Background="#888888"
                    Foreground="#000000"
                    IsEnabled="False"/>

        </WrapPanel>
    </Grid>
</Window>