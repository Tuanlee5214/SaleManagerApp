<Window x:Class="SaleManagerApp.Views.OrderDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:SaleManagerApp.ViewModels" 
        d:DataContext="{d:DesignInstance Type=viewmodels:OrderDetailViewModelWin}"
        mc:Ignorable="d"
        Title="Chi tiết hóa đơn" Height="450" Width="470" 
        WindowStartupLocation="CenterScreen" Background="#FFFFFF">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="CHI TIẾT ĐƠN HÀNG" HorizontalAlignment="Center" FontSize="20" FontWeight="Bold" Foreground="#2C3E50"/>
            <Rectangle Height="2" Fill="#666" Margin="0,10" HorizontalAlignment="Stretch"/>
            <DockPanel>
                <TextBlock Text="{Binding OrderId, StringFormat='Mã đơn: {0}'}" FontWeight="Bold"  Foreground="#7F8C8D"/>
                <TextBlock Text="{Binding CreatedAt, StringFormat='Ngày: {0:dd/MM/yyyy HH:mm}'}"  FontWeight="Bold"
                           HorizontalAlignment="Right" Foreground="#7F8C8D"/>
            </DockPanel>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Details}" 
                  AutoGenerateColumns="False" IsReadOnly="True" 
                  Background="Transparent" BorderThickness="0"
                  HeadersVisibility="Column" GridLinesVisibility="None"
                  RowBackground="Transparent" AlternatingRowBackground="#FAFAFA"
                  CanUserAddRows="False" SelectionUnit="FullRow">

            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="#4A7BF7"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Padding" Value="0,0,0,10"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1px"/>
                    <Setter Property="BorderBrush" Value="#ECF0F1"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Padding" Value="10"/>
                </Style>
            </DataGrid.RowStyle>

            <DataGrid.Columns>
                <DataGridTextColumn Header="MÓN ĂN" Binding="{Binding menuItemName}" FontWeight="SemiBold" Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Margin" Value="0,10"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="SL"  Binding="{Binding quantity}" FontWeight="SemiBold" Width="40" >
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Margin" Value="0,10"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="ĐƠN GIÁ" Binding="{Binding unitPrice, StringFormat='{}{0:N0}'}" FontWeight="SemiBold" Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Margin" Value="0,10"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="TỔNG" Binding="{Binding total, StringFormat='{}{0:N0}'}" FontWeight="SemiBold" Width="80">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Margin="0,20,0,0">
            <Rectangle Height="1" Fill="#ECF0F1" Margin="0,0,0,15"/>
            <DockPanel>
                <TextBlock Text="TỔNG CỘNG" FontSize="16" FontWeight="Bold" Foreground="#4A7BF7"/>
                <TextBlock Text="{Binding TotalMoney, StringFormat='{}{0:N0} VNĐ'}" 
                           FontSize="18" FontWeight="DemiBold" Foreground="Black" 
                           HorizontalAlignment="Right"/>
            </DockPanel>

            <Button Content="ĐÓNG" Margin="0,20,0,0" Width="100" Height="35"
                    Background="#4A7BF7" Foreground="White" BorderThickness="0"
                    Click="Close_Click"
                    >
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>
    </Grid>
</Window>